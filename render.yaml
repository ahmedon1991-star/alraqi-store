/**
 * @type {import('vite').UserConfig}
 */
export default {
  // Config for Render deployment
  build: {
    outDir: 'dist/public',
    emptyOutDir: true,
  },
  server: {
    port: process.env.PORT || 5000,
    host: '0.0.0.0'
  }
};
